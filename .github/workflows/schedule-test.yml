name: Schedule test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  test-schedule:
    runs-on: ubuntu-latest

    steps:
      - name: Print time
        run: |
          echo "Schedule test ran at:"
          date -u
